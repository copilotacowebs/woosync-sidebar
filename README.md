# WooSync Sidebar

React app for WooSync Google Sheets sidebar, built with Vite. Builds to a single `index.html` file for Google Apps Script compatibility.

## Getting Started

```bash
npm install
npm run dev      # local dev server
npm run build    # produces single dist/index.html
```